export enum RoleMember {
  ADMIN = 'admin',
  USER = 'user',
}

export enum Gender {
  MALE = 'male',
  FEMALE = 'female',
  OTHER = 'other',
}

export enum LinkType {
  MEMBER = 'member',

  // Link type for entity Images
  WORK_BRANCH = 'workBranch',
  STORE = 'store',
}
